// const soma=function(v1,v2){return v1+v2}
// console.log(soma(5,2))

const soma=(v1,v2)=>{return v1+v2}
console.log(soma(5,2))

const nome=n=>{return n} // uma única variável não precisa de parênteses.
console.log(nome("Bruno"))

const add=num=>num+10 // uma única variável também não precisa de return (operações simples)
console.log(add(10))